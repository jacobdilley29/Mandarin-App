import { ComingSoon } from "../components/ComingSoon";

export default function Learn() {
  return (
    <ComingSoon
      han="學"
      title="Learn"
      phase={1}
      tone={1}
      blurb="Structured HelloChinese-style lessons — units, vocab cards, grammar points, drills, and dialogues set in Taiwan daily life. Arrives in Phase 1 with the content pipeline and edge-tts audio."
    />
  );
}
