import { ComingSoon } from "../components/ComingSoon";

export default function Listen() {
  return (
    <ComingSoon
      han="聽"
      title="Listen"
      phase={3}
      tone={4}
      blurb="Dictation, comprehension sets, and tone ear-training (including the 20 tone pairs), all with cached zh-TW audio. Arrives in Phase 3."
    />
  );
}
